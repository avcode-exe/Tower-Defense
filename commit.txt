v1.5.2-beta.1 — Placement Preview Enhancement (Milestone 4)

Placement preview:
- Add DPS display for damaging troops (melee/ranged) in placement ghost
- Add getPlacementInvalidReason() on Game class returning human-readable
  strings: "Not enough gold", "Cannot build here", "Tile occupied"
- Show invalid placement reason text in red below the ghost tile
- DPS shown in gold (#f39c12), HPS shown in green (#2ecc71)
- Preview logic branches: invalid → reason, support → HPS, damaging → DPS

Tests:
- Add getDpsForPlacementPreview tests (melee, ranged, sniper, support, null)
- Add getPlacementInvalidReason tests (valid, gold, devMode, buildable,
  occupied, dead troops, check ordering)
- Expand placementPreview.test.js from 3 to 15 tests
