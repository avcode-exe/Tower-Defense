1.3.0-beta.1 — Ice Wizard + Bug Fixes + Runtime Stability

New Features:
- **Ice Wizard** (hotkey 0): 10th troop type, ranged attacker with splash
  that applies slow on hit. Cost: 200g, Base HP: 60, Damage: 6, Range: 3,
  Attack Speed: 1.4s, **Splash: 1.5 tiles**
- **Slow Status Effect**: Monsters hit by Ice Wizard are slowed to 50% movement
  speed for 2.5 seconds. Visual indicator: icy blue overlay on slowed monsters
  + particle burst on slow application.
- **Shatter Mechanic**: The next hit on a slowed monster deals +50% bonus
  damage. Re-applying slow refreshes duration but does NOT re-arm shatter.
- **Shielded Immunity**: Monsters with active shield are immune to slow until
  shield breaks.
- **Splash Applies Slow**: All monsters hit by splash take damage AND are slowed.
- **5th Upgrade Stat (SLW)**: Ice Wizard has a 4th upgradable stat — SLW
  increases slow potency (factor), duration, and shatter bonus per level:
  - L1: 50% speed, 2.5s, +50% dmg
  - L5: 26% speed (74% slow), 5.2s, +143% dmg

Balance:
- Added 4 new CONFIG constants for slow/shatter scaling:
  SLOW_FACTOR_SCALE_PER_LEVEL=0.85, SLOW_DURATION_SCALE_PER_LEVEL=1.2,
  SHATTER_BONUS_SCALE_PER_LEVEL=1.3, SHATTER_WINDOW=3.0s
- Upgrade cost follows existing formula (base × 1.35^(level-1))

UI Fixes:
- **SLW upgrade button click handler fixed** — was missing 'slow' from stats array
- **Dynamic troop stats panel** — auto-sizes based on visible stat lines (no text overlap)
- **Dynamic shop card area** — bottom boundary adjusts per troop (Ice Wizard's extra SLW line no longer blocks shop cards)
- **Load progress dialog** — shows popup asking to load previous progress or start new game
- **Removed hotkeys** — eliminated number-key hotkeys (0-9) that interfered with normal keyboard input; troop selection now click-only

Critical Bug Fixes:
- **Troop constructor now calls `_recomputeStats()`** — cached stats (maxHp, slowFactor, etc.) now properly initialized at spawn instead of waiting for first upgrade
- **Save/Restore includes `slowLevel`** — Ice Wizard slow upgrades now persist across save/load (was lost on reload)
- **`getTotalInvested()` includes `slow` upgrades** — sell value now accounts for slow upgrades correctly
- **`persistCollapsed()` function added** — fixed ReferenceError when toggling UI panels
- **Load game error handling** — wrapped in try/catch to prevent startup crashes
- **Null guards in update progress UI** — prevents crashes if progress elements missing
- **Optional chaining for Electron IPC** — prevents TypeError if APIs unavailable
- **Unified pre-release detection** — fixed inconsistent filtering between main/renderer processes
- **Settings structure validation** — guards against undefined properties

Auto-Update Fixes:
- `getAnnouncedVersion()` added to UpdateManager — enables skip functionality
- Skipped versions now persisted to settings — survives app restart
- `autoDownload`/`autoInstallOnAppQuit` initialized from settings on startup
- `checkOnStartup`/`checkIntervalMinutes` settings now respected
- `not-available` handler added — hides dialog when no updates found
- `releaseType: "release"` removed from package.json — lets electron-builder auto-detect prerelease

Visual:
- Slowed monsters render with semi-transparent icy blue overlay (#7fdbff @ 50%)
- Particle burst on slow application (ice blue particles)
- All splash targets get slow applied + particle effect
- Hotkey badges removed from shop cards

Modified:
- js/config.js (Ice Wizard spec with splash: 1.5, damage: 6, 4 CONFIG constants, projectile style, removed hotkeys)
- js/troop.js (slowLevel, cached slow stats init in constructor, upgrade handling, getTotalInvested includes slow)
- js/monster.js (slowTimer, baseSpeed, shatterArmed, shatterBonus, applySlow with shield immunity, _slowColorTint)
- js/game.js (applyProjectileImpact: slow on all splash/chain/direct hits, particle spawn, icy overlay render; upgrade click handler includes 'slow'; save/restore includes slowLevel; removed hotkey handling)
- js/particles.js (slowApply particle effect config)
- js/ui.js (SLW button, dynamic stat panel, dynamic _cardAreaBottom, stat labels/colors, load dialog integration, removed hotkey badges)
- js/main.js (load progress dialog with Load/Start New buttons, persistCollapsed(), try/catch loadGame, UI_LAYOUT guard)
- js/updateManager.js (getAnnouncedVersion(), not-available handler, fixed regex, null guards, optional chaining, deep clone settings)
- electron-main.js (applyAutoUpdaterSettings(), persist skipped versions, respect checkOnStartup/checkIntervalMinutes, unified pre-release regex)
- index.html (load progress dialog HTML, removed hotkey list from Controls popup)
- README.md (troops table, features list, upgrade stats, controls, removed hotkey references)
- package.json (version 1.3.0-beta.1, removed releaseType)
- commit.txt (this changelog)