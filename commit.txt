Clean up dead code and minor bugs

- Remove _activeShieldCount and _tileIndexStep dead state from
  Game (written in 4 and 2 places respectively, never read).
- Fix makeRNG in utils.js: the `??` after `>>>` never fell through
  when seed was undefined, so unseeded games always used seed 0.
  Now correctly falls back to a random int via Math.floor.
- Replace 4 occurrences of `6.2832` with `Math.PI * 2` in game.js
  for consistency with the rest of the codebase.
- Re-indent dev-right click handler so the if-body is visibly
  nested inside the for-j loop (was dedented, misleading).
