UI polish: fix card scroll bleed and maxed stat button visibility

- Move _updateCardAreaBottom() call before card clip rect computation
  so the scroll area boundary is correct on the first frame
- Add solid mask below card scroll area to prevent cards from visually
  bleeding into the stats/upgrade panel
- Improve maxed stat upgrade button visibility: background opacity 0.04→0.08
  with 1px border so MAX buttons are distinguishable from the panel
