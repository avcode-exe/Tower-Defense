v1.1.1: architecture cleanup — scope, constants, and safety hardening

MEDIUM impact:
- Replace Monster._dmgResult shared mutable state with fresh objects per call (monster.js)
- Scope NEIGHBORS into generatePath() to eliminate module-level leak (pathGenerator.js)
- IIFE-wrap ui.js to privatize UI_COLORS and helpers; expose only UI, UI_LAYOUT,
  UIRoundRect, drawToggleButton, hitToggleButton as globals (ui.js)
- Move SPEEDS to CONFIG.GAME_SPEEDS for consistent global config access (config.js, ui.js, game.js)

LOW impact:
- Extract particle gravity magic number 60 into CONFIG.PARTICLE_GRAVITY (config.js, particles.js)
- Extract HP bar and shield bar color literals into CONFIG.COLORS (config.js, game.js)
- Replace 8 magic +0.5 tile-buffer literals with CONFIG.TILE_BUFFER (config.js, troop.js, ui.js)

Zero behavioral changes — all refactoring verified via syntax check across all 15 JS modules.
