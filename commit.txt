v1.5.2-beta.1 — Pre-release: auto-update fix for prerelease→stable

Version bump:
- Bump version from 1.5.1 to 1.5.2-beta.1 across package.json,
  package-lock.json, gamePersistence.js, main.js, updateManager.js,
  and all test assertions

Auto-update fix:
- Fix pre-release channel users unable to download newer stable releases
  (e.g., 1.5.1-beta.1 → 1.5.1) due to Atom feed tag "v" prefix mismatch
- Add selectNewestNewerRelease() to githubReleaseFeed.js that matches
  both stable and prerelease entries from the Atom feed
- Add resolveDownloadTag() that probes HEAD latest.yml with and without
  "v" prefix to find the correct GitHub release tag for download URLs
- Update electron-main.js patched getUpdateInfoAndProvider to try
  stable releases after prerelease scan yields nothing
- Always fetch latest.yml (not beta.yml) in custom update provider

Cleanup:
- Delete stale git tag 1.5.1 (no "v") that caused Atom feed duplicates
